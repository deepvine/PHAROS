# =============================================================
# PHAROS — minimal runtime dependencies
# (Original KnowAgent self-learning track requires more; see notes)
# =============================================================

# Core LLM stack
openai>=1.0.0
langchain>=0.2,<0.3
langchain-openai>=0.1,<0.2
langchain-community>=0.2,<0.3

# Search backends
tavily-python>=0.3
wikipedia>=1.4.0

# Datasets
datasets<3.0
joblib>=1.3
pandas>=2.0

# Utilities
python-dotenv>=1.0
tiktoken>=0.5
pypdf>=4.0

# =============================================================
# Optional: KnowAgent self-learning track
# (Not used by PHAROS prompting-only pipeline)
# =============================================================
# fschat==0.2.35
# transformers==4.36.2
# peft==0.5.0
# accelerate==0.23.0
# deepspeed==0.11.1
# flash_attn==2.3.2

# =============================================================
# Optional: ALFWorld benchmark
# (TextWorld engine requires Inform7; not buildable on ARM64)
# =============================================================
# alfworld==0.3.1
